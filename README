xc - xnacly's word counter
==========================
very small clone of wc

Installation
------------

       make install


Run xc
------
       xc -h

Options
-------
After installation run man xc or xc -h.

       -v, --version
       -m, --chars
       -l, --lines
       -w, --words
       -h, --help
